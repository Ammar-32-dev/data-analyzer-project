# from django import forms

# class CSVUploadForm(forms.Form):
#     csv_file = forms.FileField()
#     recipient_email = forms.EmailField(label="Recipient Email", required=False, help_text="Optional: Enter an email to receive analysis results.")